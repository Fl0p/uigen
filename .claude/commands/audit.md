Your goal is to audit and fix security vulnerabilities in npm dependencies.

Run `npm audit` to check for security vulnerabilities in dependencies and show the results.
Run `npm audit fix` to fix the vulnerabilities automatically where possible.
Run the tests and verify the updates didn't break anything
Do nessessary fixes if needed
Review the audit report and provide a summary of the findings and actions taken.